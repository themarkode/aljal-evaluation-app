rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write access to evaluations collection
    // This is for internal company app - adjust security later if needed
    match /evaluations/{document=**} {
      allow read, write: if true;
    }
  }
}
